import 'package:flutter/material.dart';

class NutritionQuickAddPlaceholderScreen extends StatelessWidget {
  const NutritionQuickAddPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Duplicar reciente')),
      body: const Center(child: Text('TODO: lista de comidas recientes para duplicar')),
    );
  }
}
