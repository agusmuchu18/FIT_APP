class MotionDurations {
  static const Duration micro = Duration(milliseconds: 180);
  static const Duration menu = Duration(milliseconds: 280);
  static const Duration check = Duration(milliseconds: 260);
  static const Duration checkHold = Duration(milliseconds: 480);
}
